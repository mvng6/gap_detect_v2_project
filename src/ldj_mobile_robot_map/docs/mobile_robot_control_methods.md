# 모바일 로봇 제어 방안 비교

## 개요

woosh 모바일 로봇(TR-200)을 ROS 환경에서 제어하기 위한 두 가지 방안을 비교합니다.

- **방안 1**: Twist 명령을 통한 직접 속도 제어
- **방안 2**: ROS Navigation Stack 연동

---

## 시스템 구성도

### 방안 1: Twist 직접 제어

```
┌─────────────────────────────────────────────────────────────────┐
│                        woosh 로봇 (TR-200)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                      │
│  │  LiDAR   │  │ Odometry │  │   맵     │                      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘                      │
└───────┼─────────────┼─────────────┼─────────────────────────────┘
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────┐
│              woosh → ROS 브릿지 노드                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                      │
│  │/scan     │  │/odom     │  │/map      │                      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘                      │
└───────┼─────────────┼─────────────┼─────────────────────────────┘
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────┐
│                         RViz (시각화)                            │
└─────────────────────────────────────────────────────────────────┘
        ▲
        │
┌───────┴─────────────────────────────────────────────────────────┐
│              사용자 정의 제어 노드                                │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  - twist_req()로 직접 속도 명령                           │   │
│  │  - 자체 경로 계획 알고리즘 구현 필요                       │   │
│  │  - 장애물 회피 로직 직접 구현                             │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 방안 2: ROS Navigation Stack 연동

```
┌─────────────────────────────────────────────────────────────────┐
│                        woosh 로봇 (TR-200)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                      │
│  │  LiDAR   │  │ Odometry │  │   맵     │                      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘                      │
└───────┼─────────────┼─────────────┼─────────────────────────────┘
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────┐
│              woosh → ROS 브릿지 노드                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │/scan     │  │/odom     │  │/map      │  │/tf       │        │
│  │LaserScan │  │Odometry  │  │OccGrid   │  │Transform │        │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘        │
└───────┼─────────────┼─────────────┼─────────────┼───────────────┘
        │             │             │             │
        ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ROS Navigation Stack                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │  amcl    │  │move_base │  │ costmap  │  │global/   │        │
│  │(위치추정)  │  │(네비게이션)│  │(장애물)   │   │local     │        │
│  │          │  │          │  │          │  │planner   │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│                         RViz (시각화)                            │
│  - 맵 시각화                                                     │
│  - LiDAR 시각화                                                  │
│  - 경로 시각화                                                   │
│  - 2D Nav Goal로 목표 지점 설정                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 기능 비교표

| 기능 | 방안 1 (Twist 직접 제어) | 방안 2 (ROS Navigation Stack) |
|------|-------------------------|-------------------------------|
| **RViz 맵 시각화** | ✅ 가능 (브릿지 노드 필요) | ✅ 가능 (브릿지 노드 필요) |
| **LiDAR 실시간 모니터링** | ✅ 가능 (브릿지 노드 필요) | ✅ 가능 (브릿지 노드 필요) |
| **현재 위치 표시** | ✅ 가능 | ✅ 가능 |
| **목표 위치 설정** | ⚠️ 코드로 직접 설정 | ✅ RViz에서 클릭으로 설정 |
| **경로 계획** | ⚠️ 자체 구현 필요 | ✅ 내장 (A*, Dijkstra, DWA 등) |
| **장애물 회피** | ⚠️ 자체 구현 필요 | ✅ 내장 (costmap, local planner) |
| **구현 난이도** | 🟢 낮음 | 🟡 중간 |
| **설정 복잡도** | 🟢 낮음 | 🟡 중간~높음 |
| **Taskable 상태 의존성** | ❌ 없음 | ❌ 없음 |
| **확장성** | 🟡 제한적 | 🟢 높음 (플러그인 활용) |

---

## 상세 비교

### 1. RViz 맵 시각화

#### 방안 1 & 방안 2 공통
두 방안 모두 **woosh → ROS 브릿지 노드**를 통해 RViz에서 맵을 시각화할 수 있습니다.

```python
# 브릿지 노드에서 맵 퍼블리시 예시
from nav_msgs.msg import OccupancyGrid

map_pub = rospy.Publisher('/map', OccupancyGrid, queue_size=1, latch=True)

# woosh BuildMapData → OccupancyGrid 변환
def publish_map(woosh_map_data):
    ros_map = OccupancyGrid()
    ros_map.header.frame_id = "map"
    ros_map.info.resolution = woosh_map_data.resolution
    ros_map.info.width = woosh_map_data.width
    ros_map.info.height = woosh_map_data.height
    ros_map.info.origin.position.x = woosh_map_data.origin_x
    ros_map.info.origin.position.y = woosh_map_data.origin_y
    ros_map.data = list(woosh_map_data.map_data)
    map_pub.publish(ros_map)
```

---

### 2. LiDAR 실시간 모니터링

#### 방안 1 & 방안 2 공통
woosh의 `ScannerData`를 ROS `LaserScan` 메시지로 변환하여 퍼블리시합니다.

```python
# 브릿지 노드에서 LiDAR 퍼블리시 예시
from sensor_msgs.msg import LaserScan

scan_pub = rospy.Publisher('/scan', LaserScan, queue_size=10)

def scanner_callback(woosh_scanner: ScannerData):
    scan = LaserScan()
    scan.header.stamp = rospy.Time.now()
    scan.header.frame_id = "laser"
    scan.angle_min = woosh_scanner.angle_min
    scan.angle_max = woosh_scanner.angle_max
    scan.angle_increment = woosh_scanner.angle_increment
    scan.time_increment = woosh_scanner.time_increment
    scan.scan_time = woosh_scanner.scan_time
    scan.range_min = woosh_scanner.range_min
    scan.range_max = woosh_scanner.range_max
    scan.ranges = list(woosh_scanner.ranges)
    scan_pub.publish(scan)
```

---

### 3. 경로 계획 알고리즘 적용

#### 방안 1: 자체 구현 필요

Twist 직접 제어 방식에서는 경로 계획 알고리즘을 **직접 구현**해야 합니다.

**적용 가능한 알고리즘:**
- A* 알고리즘
- Dijkstra 알고리즘
- RRT (Rapidly-exploring Random Tree)
- Potential Field Method

```python
# 방안 1: 자체 경로 계획 예시
class PathPlanner:
    def __init__(self, map_data):
        self.map = map_data
    
    def plan_path(self, start, goal):
        # A* 알고리즘 구현
        path = self.a_star(start, goal)
        return path
    
    def follow_path(self, path):
        for waypoint in path:
            # 각 waypoint로 Twist 명령 생성
            twist = self.compute_twist(waypoint)
            await self.robot.twist_req(twist, NO_PRINT, NO_PRINT)
```

**장점:**
- 완전한 커스터마이징 가능
- 특정 요구사항에 최적화 가능

**단점:**
- 개발 시간 소요
- 검증 및 테스트 필요

---

#### 방안 2: ROS Navigation Stack 활용

ROS Navigation Stack은 **검증된 경로 계획 알고리즘**을 제공합니다.

**내장된 플래너:**

| 플래너 | 유형 | 알고리즘 |
|--------|------|----------|
| `navfn` | Global Planner | Dijkstra / A* |
| `global_planner` | Global Planner | A* (Flexible) |
| `carrot_planner` | Global Planner | 직선 경로 |
| `dwa_local_planner` | Local Planner | Dynamic Window Approach |
| `teb_local_planner` | Local Planner | Timed Elastic Band |
| `mpc_local_planner` | Local Planner | Model Predictive Control |

**설정 예시 (move_base_params.yaml):**
```yaml
base_global_planner: "global_planner/GlobalPlanner"
base_local_planner: "dwa_local_planner/DWAPlannerROS"

GlobalPlanner:
  allow_unknown: true
  default_tolerance: 0.0
  use_dijkstra: false  # A* 사용
  use_grid_path: false

DWAPlannerROS:
  max_vel_x: 0.5
  min_vel_x: 0.0
  max_vel_theta: 1.0
  acc_lim_x: 2.5
  acc_lim_theta: 3.2
```

**장점:**
- 즉시 사용 가능
- 검증된 알고리즘
- 다양한 플러그인 지원

**단점:**
- 초기 설정 복잡
- 파라미터 튜닝 필요

---

## 구현 로드맵

### 방안 1 구현 순서

```
1. woosh → ROS 브릿지 노드 생성 (필수)
   ├── ScannerData → /scan
   ├── PoseSpeed → /odom + TF
   └── BuildMapData → /map

2. RViz 설정
   └── 맵, LiDAR, 로봇 위치 시각화

3. Twist 제어 노드 개발
   └── 기본 전진/후진/회전 제어

4. (선택) 자체 경로 계획 알고리즘 구현
   ├── A* / Dijkstra 구현
   └── 장애물 회피 로직
```

### 방안 2 구현 순서

```
1. woosh → ROS 브릿지 노드 생성 (필수)
   ├── ScannerData → /scan
   ├── PoseSpeed → /odom + TF
   ├── BuildMapData → /map
   └── Twist 명령 수신 → woosh twist_req

2. TF 설정
   └── map → odom → base_link → laser

3. Navigation Stack 설치 및 설정
   ├── move_base 설정
   ├── amcl 설정 (또는 로컬라이제이션 비활성화)
   ├── costmap 설정
   └── 플래너 파라미터 튜닝

4. RViz 설정
   └── 2D Nav Goal로 목표 지점 설정 가능

5. 테스트 및 파라미터 튜닝
```

---

## 결론 및 권장사항

### 단기 목표 (빠른 동작 확인)
**→ 방안 1 권장**

- Twist 명령으로 즉시 동작 확인 가능
- `ldj_load_map.py` 기반으로 빠르게 테스트 가능
- 맵 로드 + 로컬라이제이션 후 직접 속도 제어

### 장기 목표 (완전한 자율 주행)
**→ 방안 2 권장**

- ROS Navigation Stack의 검증된 알고리즘 활용
- RViz에서 클릭만으로 목표 지점 설정
- 다양한 경로 계획 알고리즘 플러그인 지원
- 장애물 회피 자동 처리

### 권장 진행 순서

1. **1단계**: 방안 1로 기본 동작 확인
   - Twist 제어로 전진/후진/회전 테스트
   - 브릿지 노드 기본 구현 (LiDAR, Odom)

2. **2단계**: 브릿지 노드 완성
   - 맵 데이터 퍼블리시
   - TF 브로드캐스트

3. **3단계**: 방안 2로 확장
   - Navigation Stack 설정
   - 경로 계획 및 자율 주행

---

## 참고 자료

- [ROS Navigation Stack Wiki](http://wiki.ros.org/navigation)
- [move_base 설정 가이드](http://wiki.ros.org/move_base)
- [costmap_2d 설정](http://wiki.ros.org/costmap_2d)
- [amcl 설정](http://wiki.ros.org/amcl)
- [DWA Local Planner](http://wiki.ros.org/dwa_local_planner)

---

## 부록: 필요한 ROS 패키지

```bash
# ROS Navigation Stack 설치 (ROS Noetic)
sudo apt-get install ros-noetic-navigation
sudo apt-get install ros-noetic-amcl
sudo apt-get install ros-noetic-move-base
sudo apt-get install ros-noetic-map-server
sudo apt-get install ros-noetic-dwa-local-planner
sudo apt-get install ros-noetic-global-planner

# TF 관련
sudo apt-get install ros-noetic-tf2-ros
sudo apt-get install ros-noetic-robot-state-publisher
```

---

*문서 작성일: 2025-12-04*
*작성자: KATECH 로봇 자동화 연구팀*

