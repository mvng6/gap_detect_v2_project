# ROS 1 Noetic: 액션(Action), 토픽(Topic), 중앙 관제탑 노드를 이용한 모바일 로봇과 두산 협동로봇 연동

이 문서는 모바일 로봇과 두산 협동로봇이 중앙 관제탑 노드에 의해 순차적으로 협력하는 시스템을 구축하는 과정을 설명합니다. 이 시스템은 Docker 환경 내의 ROS 1 Noetic을 기반으로 합니다.

## 🎯 **최종 목표**

다음과 같은 순환 작업을 구현합니다:
1.  모바일 로봇이 목표 위치로 이동합니다.
2.  모바일 로봇이 정지하면, 두산 협동로봇이 미리 정의된 자세로 이동했다가 초기 자세로 복귀합니다.
3.  협동로봇이 초기 자세로 복귀하면, 모바일 로봇이 다시 이동합니다.

## ⚙️ **핵심 구성 요소**

1.  **모바일 로봇 노드:** 자체 SDK를 사용하여 모바일 로봇의 움직임을 제어합니다. 이동 작업에 대한 액션 서버(Action server)를 제공하고, 자신의 상태(이동 중/정지)를 토픽(Topic)으로 발행(Publish)합니다.
2.  **두산 협동로봇 노드:** 제공된 ROS 패키지(`movej`)를 사용하여 두산 로봇 팔을 제어합니다. 팔 움직임 작업에 대한 액션 서버를 제공하며(먼저 모바일 로봇 상태 확인), 자신의 상태(이동 중/초기 자세)를 토픽으로 발행합니다.
3.  **중앙 관제탑 노드 (신규):** 전체 과정을 지휘하는 "지휘자" 🎻입니다. 다른 두 노드에게 명령(액션 목표, Action Goals)을 보내고, 그 결과를 감시하여 다음 단계를 결정합니다.

---

## 📝 **1단계: 통신 인터페이스 정의**

먼저 로봇들과 관제탑이 서로 어떻게 "대화"할지 방법을 정해야 합니다.

1.  **액션 명세 정의 (`.action` 파일):**
    * **목표 (Goal):** 어떤 작업을 해야 하는가?
    * **피드백 (Feedback):** 작업 중 진행 상황은 어떤가?
    * **결과 (Result):** 작업이 성공했는가? 결과는 무엇인가?

    * **`MoveMobile.action` (모바일 로봇용):**
        ```action
        # 목표 정의
        geometry_msgs/Pose target_pose # 이동할 목표 위치
        ---
        # 결과 정의
        bool success # 목표 도달 시 True
        ---
        # 피드백 정의
        float32 distance_remaining # 남은 거리
        ```
    * **`MoveArmSequence.action` (두산 협동로봇용):**
        ```action
        # 목표 정의
        int32 sequence_id # 실행할 사전 정의된 순서 (예: 1번은 주요 작업)
        ---
        # 결과 정의
        bool success # 순서 완료 시 True
        ---
        # 피드백 정의
        string status # 예: "목표 지점으로 이동 중", "초기 자세로 복귀 중"
        ```

2.  **상태 토픽 메시지 정의 (`.msg` 파일 또는 표준 메시지):**
    * 각 로봇의 *현재 상태*를 계속 보고할 메시지가 필요합니다. 표준 ROS 메시지로 충분할 수도 있고, 사용자 정의 메시지를 만들 수도 있습니다. 여기서는 간단하게 `std_msgs/String`을 사용합니다.

    * **토픽:** `/mobile_robot/status` (메시지 타입: `std_msgs/String`)
        * 가능한 값: `"MOVING"`, `"STOPPED"`
    * **토픽:** `/doosan_robot/status` (메시지 타입: `std_msgs/String`)
        * 가능한 값: `"MOVING"`, `"IDLE_HOME"` (초기 자세에서 대기 중)

3.  **인터페이스용 새 패키지 생성 (선택 사항이지만 권장):**
    * 사용자 정의 `.action` 및 `.msg` 파일을 별도의 패키지(예: `robot_interfaces`)에 모아두는 것이 좋습니다.
    * **"ROS 작업실"(Docker) 내부에서:**
        ```bash
        cd /root/catkin_ws/src
        catkin_create_pkg robot_interfaces std_msgs geometry_msgs actionlib_msgs message_generation actionlib
        # robot_interfaces/action/ 폴더 안에 .action 파일 생성
        # robot_interfaces/package.xml 과 CMakeLists.txt 를 수정하여 액션/메시지 빌드 설정
        cd /root/catkin_ws
        catkin_make
        source devel/setup.bash
        ```

---

## 💻 **2단계: 모바일 로봇 노드 구현**

모바일 로봇을 제어하는 노드를 수정하거나 새로 만듭니다.

1.  **액션 서버 (`MoveMobile`):**
    * `actionlib` SimpleActionServer를 구현합니다.
    * `goal` (target_pose)을 받으면:
        * SDK를 사용하여 로봇 이동을 시작합니다.
        * 주기적으로 `feedback` (distance_remaining)을 발행합니다.
        * `/mobile_robot/status` 토픽에 `"MOVING"` 상태를 발행합니다.
    * 이동이 완료되거나 실패하면:
        * `result` (success=True 또는 False)를 전송합니다.
        * `/mobile_robot/status` 토픽에 `"STOPPED"` 상태를 발행합니다.

2.  **상태 발행자 (Publisher):**
    * `/mobile_robot/status` (`std_msgs/String`)를 위한 `ros::Publisher`를 생성합니다.
    * 로봇의 SDK 상태를 주기적으로 확인하고 `"MOVING"` 또는 `"STOPPED"`를 발행합니다.

3.  **코드 위치:** 이 로직은 일반적으로 모바일 로봇 제어 전용 패키지(예: `mobile_robot_control`) 내의 C++ 또는 파이썬 노드에 들어갑니다. 해당 패키지의 `package.xml`과 `CMakeLists.txt`에 필요한 의존성(`robot_interfaces`, `actionlib`, `geometry_msgs`, `std_msgs`)을 추가하는 것을 잊지 마세요.

---

## 🦾 **3단계: 두산 협동로봇 노드 구현**

두산 로봇의 기능을 감싸는 노드를 수정하거나 새로 만듭니다. `doosan_helper` 패키지를 재사용/확장해 보겠습니다.

1.  **액션 서버 (`MoveArmSequence`):**
    * `doosan_helper/src` 안에 새로운 C++ 노드(예: `doosan_action_server.cpp`)에서 `actionlib` SimpleActionServer를 구현합니다.
    * `goal` (sequence_id)을 받으면:
        * **[중요!]** `/mobile_robot/status` 토픽을 *일시적*으로 구독하거나 최신 메시지를 확인하여, 상태가 `"STOPPED"`가 될 때까지 기다립니다.
        * 정지가 확인되면 `/doosan_robot/status` 토픽에 `"MOVING"` 상태를 발행합니다.
        * `movej` 서비스(`move_robot_node.cpp`에서처럼)를 호출하여 `[90, 0, 90, 0, 90, -90]` 자세로 이동합니다.
        * `feedback`으로 `"Moving to target"`을 발행합니다.
        * 다시 `movej`를 호출하여 초기 자세(예: `[0, 0, 0, 0, 0, 0]`)로 복귀합니다.
        * `feedback`으로 `"Returning to home"`을 발행합니다.
        * `result` (success=True)를 전송합니다.
        * `/doosan_robot/status` 토픽에 `"IDLE_HOME"` 상태를 발행합니다.
    * 만약 일정 시간 내에 모바일 로봇이 멈추지 않거나 `movej` 호출이 실패하면, `result` (success=False)를 전송합니다.

2.  **상태 발행자 (Publisher):**
    * `/doosan_robot/status` (`std_msgs/String`)를 위한 `ros::Publisher`를 생성합니다.
    * 액션 서버가 움직임을 시작하면 `"MOVING"`을 발행하고, 끝나거나 대기 중일 때는 `"IDLE_HOME"`을 발행합니다. (더 확실하게 하려면 로봇의 실제 관절 상태를 조회하는 것이 좋습니다.)

3.  **코드 위치 및 빌드:**
    * 새로운 C++ 노드(`doosan_action_server.cpp`)를 `doosan_helper/src`에 추가합니다.
    * 필요한 헤더 파일(`actionlib/server/simple_action_server.h`, `robot_interfaces/MoveArmSequenceAction.h`, `std_msgs/String.h` 등)을 포함시킵니다.
    * `doosan_helper/CMakeLists.txt`를 수정하여 새 실행 파일을 추가합니다:
        ```cmake
        add_executable(doosan_action_server src/doosan_action_server.cpp)
        target_link_libraries(doosan_action_server ${catkin_LIBRARIES})
        ```
    * `doosan_helper/package.xml`에 필요한 의존성(`robot_interfaces`, `actionlib`, `std_msgs`)을 추가합니다.
    * **"ROS 작업실"(Docker) 내부에서:**
        ```bash
        cd /root/catkin_ws
        catkin_make
        source devel/setup.bash
        ```

---

## 🧠 **4단계: 중앙 관제탑 노드 구현**

이것이 새로운 "지휘자" 노드입니다.

1.  **새 패키지 생성:**
    * **"ROS 작업실"(Docker) 내부에서:**
        ```bash
        cd /root/catkin_ws/src
        # 의존성: roscpp, actionlib, robot_interfaces, std_msgs
        catkin_create_pkg robot_coordinator roscpp actionlib robot_interfaces std_msgs
        ```

2.  **관제탑 로직 작성 (예: `coordinator_node.cpp` in `robot_coordinator/src`):**
    * ROS 노드를 초기화합니다.
    * `/move_mobile` (모바일 로봇)과 `/move_arm_sequence` (두산 협동로봇) 모두에 대한 **액션 클라이언트(Action Clients)**를 생성합니다. 두 서버가 준비될 때까지 기다립니다 (`waitForServer`).
    * `/mobile_robot/status`와 `/doosan_robot/status`를 위한 **구독자(Subscribers)**를 생성합니다. 각 로봇의 최신 상태를 저장합니다.
    * 메인 루프 또는 상태 기계(State Machine)를 구현합니다:
        * **상태 1: 모바일 로봇 이동:**
            * `MoveMobileGoal`을 생성합니다.
            * 모바일 로봇 액션 클라이언트를 사용하여 목표를 전송합니다 (`sendGoal`).
            * 결과를 기다립니다 (`waitForResult`).
        * **상태 2: 팔 상태 확인 및 팔 이동:**
            * 모바일 로봇이 성공했다면, 최신 `/doosan_robot/status`를 확인합니다. 만약 `"IDLE_HOME"`이면 다음 단계로 진행합니다.
            * `MoveArmSequenceGoal`을 생성합니다.
            * 두산 액션 클라이언트를 사용하여 목표를 전송합니다.
            * 결과를 기다립니다.
        * **상태 3: 모바일 상태 확인 및 반복:**
            * 팔 움직임이 성공했다면, 최신 `/mobile_robot/status`를 확인합니다. 만약 `"STOPPED"`이면 상태 1로 돌아갑니다.
        * 액션 실패 시의 오류 처리를 포함시킵니다.

3.  **관제탑 패키지 빌드:**
    * `robot_coordinator/CMakeLists.txt`를 수정하여 실행 파일을 추가합니다.
    * **"ROS 작업실"(Docker) 내부에서:**
        ```bash
        cd /root/catkin_ws
        catkin_make
        source devel/setup.bash
        ```

---

## ✅ **5단계: 통합 및 테스트**

모든 것을 함께 실행합니다.

1.  **런치 파일(.launch) 생성:** 필요한 모든 노드를 쉽게 시작할 수 있도록 ROS 런치 파일을 만듭니다:
    * 두산 드라이버 (`dsr_moveit.launch` 또는 유사 파일).
    * 모바일 로봇 드라이버/SDK 노드.
    * 모바일 로봇 액션 서버 + 상태 발행 노드.
    * 두산 액션 서버 + 상태 발행 노드 (`doosan_action_server`).
    * 관제탑 노드 (`coordinator_node`).

2.  **실행 (별도의 "ROS 작업실" 터미널들에서):**
    * **터미널 1:** 두산 드라이버 실행.
        ```bash
        # (Docker)
        roslaunch dsr_launcher dsr_moveit.launch model:=a0912 mode:=real host:=192.168.137.100
        ```
    * **터미널 2:** 모바일 로봇 드라이버 및 노드 실행.
        ```bash
        # (Docker)
        # (모바일 로봇 패키지에 따라 명령어는 달라짐)
        roslaunch mobile_robot_control mobile_robot.launch
        ```
    * **터미널 3:** 두산 액션 서버 노드 실행.
        ```bash
        # (Docker)
        source /root/catkin_ws/devel/setup.bash
        rosrun doosan_helper doosan_action_server
        ```
    * **터미널 4:** 관제탑 노드 실행.
        ```bash
        # (Docker)
        source /root/catkin_ws/devel/setup.bash
        rosrun robot_coordinator coordinator_node
        ```

3.  **관찰 및 디버깅:** 로봇의 움직임과 ROS 로그(`rostopic echo /mobile_robot/status`, `rostopic echo /doosan_robot/status` 등)를 보면서 순서가 올바르게 실행되는지 확인합니다. 노드 연결을 시각화하려면 `rqt_graph` 같은 도구를 사용합니다.

이 상세한 과정은 사용자님이 설명한 협동 작업을 달성하기 위한 강력하고 표준적인 ROS 방식을 제공합니다. 코드에 잠재적인 오류나 예외 상황 처리를 포함하는 것을 잊지 마세요!